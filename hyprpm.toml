[repository]
name = "hyprland-master-stack"
authors = ["Pedro"]
commit_pins = []

[master-stack]
description = "Master + card-stack layout: master pinned on the left, card-stack of slaves on the right with peek strips"
authors = ["Pedro"]
output = "master-stack.so"
build = [
    "make all",
]
